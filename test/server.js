import * as chai from "chai"
const expect = chai.expect

describe('Soma', () => {
    it("Somas", function(){
        const resultado = 0 + 5
        expect(resultado).be.equal(5)
        done()
    })
})